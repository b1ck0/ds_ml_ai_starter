#!/usr/bin/env ruby
# bin/rails — the binstub every `rails <command>` in this README ultimately runs. Auto-generated by
# `rails new` verbatim; NOT hand-edited beyond what `rails new` itself would produce.
# NOTE-SDLC-4-ADD2-docker.md §4.
APP_PATH = File.expand_path("../config/application", __dir__)
require_relative "../config/boot"
require "rails/commands"
