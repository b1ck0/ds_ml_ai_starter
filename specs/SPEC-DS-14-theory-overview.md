# SPEC-DS-14: Data Science Theory — the concepts behind the code

**Status:** done (written by Sonnet, grounded by Haiku, independently reviewed + merged 2026-09-03)
**Subject:** Data Science
**Section:** Theory
**Routing:** writer=Sonnet 4.6 · research=Haiku · review=Sonnet (fresh) · architect=Opus 4.8
**Prerequisites:** none — this is the conceptual map; it forward-links to every worked example.

## Intent
A single, navigable theory chapter that defines the vocabulary and ideas the worked examples put into
practice, so the reader has a map before diving into code. Each concept is short, grounded, and links
to the chapter that demonstrates it.

## Learning objectives
- LO1 — Define and relate: hypothesis testing, imputation, feature engineering, regression, classification (binary/multi-class/multi-label).
- LO2 — Define: class imbalance, undersampling, ensembles (bagging vs boosting), forecasting.
- LO3 — Define: AutoML, feature selection, overfitting, regularization (L1/L2) and the bias–variance trade-off.
- LO4 — Navigate: each concept links to its Worked Examples chapter.

## Scope
In: crisp definitions + intuition + the "why it matters" for every Theory-list item, with forward links.
Out: full derivations (kept light; deep math deferred to the relevant worked example or a reference link).

## Outline
1. The shape of a supervised-learning problem — X, y, train/serve, generalisation.
2. Statistics foundations — hypothesis testing, p-values, effect size.
3. Data preparation — imputation, feature engineering, encoding, scaling, collinearity.
4. Models — regression vs classification; linear models, trees, ensembles (bagging vs boosting).
5. The central tension — overfitting, bias–variance, regularization (L1/L2), why fewer features help.
6. Beyond the basics — class imbalance, forecasting, feature selection, AutoML — each with a forward link.

## Assets to produce
- Prose: "Data Science/Theory/theory-overview.md"
- Artefacts: a bias–variance illustration + an overfitting (train vs valid error vs complexity) plot generated by a small script.
- Code: "Data Science/Theory/code/bias_variance_overfitting.py" (produces the two illustrative plots).

## Claims to ground (Haiku, before writing)
- [ ] Verify the definitions used for regularization (L1 vs L2 effect on coefficients), the bias–variance decomposition, and bagging-vs-boosting against authoritative sources.
- [ ] Reuse NOTE-5 for the small sklearn script that generates the overfitting curve.

## Acceptance criteria
- [ ] AC1 — every Theory-list concept defined with intuition + forward link. AC2 — the illustrative script runs and produces both plots; snippet-check passes. AC3 — definitions grounded. AC4 — written as a map a Java dev can skim before the worked examples; jargon explained on first use.

## Gates
Entry: approved; notes landed. Exit: DoD checklist.
