# SPEC-DS-15: Managed ML Platforms — Vertex AI, Azure ML, SageMaker

**Status:** done (written by Sonnet, grounded by Haiku, independently reviewed + merged 2026-09-03)
**Subject:** Data Science
**Section:** Cloud Environment Setup
**Routing:** writer=Sonnet 4.6 · research=Haiku · review=Sonnet (fresh) · architect=Opus 4.8
**Prerequisites:** SPEC-DS-5/DS-6 (a model to deploy), SPEC-DS-12 (registry)
**Nature:** GROUNDED CONCEPTUAL — cloud services cannot execute in the sandbox. Code is real, verified
CLI/SDK snippets fenced as reference (```text or ```python-pseudocode), never fabricated output.

## Intent
Show why teams graduate from a laptop notebook to a managed ML platform, and map the SAME workflow
(notebooks → training pipelines → model registry → deployment) across the three big clouds so a Java
dev can recognise the equivalent service in each.

## Learning objectives
- LO1 — Explain the MLOps advantages of a managed platform over ad-hoc notebooks (reproducible pipelines, lineage, scaling, governance).
- LO2 — Map the workflow across clouds: managed notebooks, training pipelines, model registry, endpoints — naming the concrete service in Vertex AI, Azure ML, and SageMaker.
- LO3 — Read a minimal training-pipeline + deploy snippet in at least one cloud's SDK.
- LO4 — Reason about cost/lock-in/portability trade-offs.

## Scope
In: side-by-side service mapping (a comparison table); managed notebooks; pipelines; registry; endpoints; a worked SDK snippet for one cloud (Vertex AI) + service-name equivalents for the others.
Out: hands-on execution (impossible in sandbox), deep per-service tutorials (link to official docs).

## Outline
1. Why leave the notebook — the MLOps gap; what breaks at team scale.
2. The universal workflow — notebook → pipeline → registry → endpoint.
3. Cross-cloud mapping TABLE — Vertex AI vs Azure ML vs SageMaker for each stage.
4. A concrete Vertex AI pipeline+deploy sketch (verified SDK names), with Azure/SageMaker equivalents noted.
5. Trade-offs — cost, lock-in, portability, when a plain container is enough.

## Claims to ground (Haiku, before writing) — service names change; VERIFY
- [ ] Verify current service names + the mapping for each stage: Vertex AI (Workbench, Vertex AI Pipelines, Model Registry, Endpoints), Azure ML (Compute Instances, Pipelines/Jobs, Model Registry, Online/Batch Endpoints), SageMaker (Studio/Notebooks, Pipelines, Model Registry, Endpoints). Confirm against current official docs (2026).
- [ ] Verify the current Vertex AI Python SDK entry points used in the snippet (e.g. google-cloud-aiplatform / `aiplatform.init`, pipeline + `Model.deploy`) from official docs — mark clearly it's reference, not executed.

## Assets to produce
- Prose: "Data Science/Cloud Environment Setup/managed-ml-platforms.md"
- Artefacts: the cross-cloud comparison table (in-prose) + a workflow diagram (matplotlib/SVG generated by a small script).

## Acceptance criteria
- [ ] AC1 — LOs delivered incl. the comparison table. AC2 — every code block is either runnable (the diagram script) or clearly fenced as reference; snippet-check passes; NO fabricated console output. AC3 — all service names + SDK entry points grounded with dated citations. AC4 — the "notebook → platform" motivation lands for an engineer who values reproducible builds.

## Gates
Entry: approved; grounding (service names verified for 2026) landed. Exit: DoD — links resolve; review; merge.
